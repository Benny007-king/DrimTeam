rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Gallery videos — public read, only authenticated users (admins) can upload
    match /gallery/videos/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
